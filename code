int motionPin = 2;
void setup()
{
  pinMode(4, OUTPUT);
  pinMode(2, INPUT);
}

void loop()
{
  int val=digitalRead(motionPin);
  if (val == HIGH)
  {
    digitalWrite(4, HIGH);
  }
  else
  {
    digitalWrite(4, LOW)
  }
}
